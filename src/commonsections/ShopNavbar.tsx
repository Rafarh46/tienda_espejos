"use client";

const ShopNavbar = () => {
  // Se elimina el submenú secundario de categorías antiguas (Accessories, Bottom, etc.).
  // La navegación por categorías ahora vive únicamente en Header > "Categorías".
  return null;
};

export default ShopNavbar;
